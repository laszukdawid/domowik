apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: domowik-ingress
  namespace: domowik
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - domowik.lasz.uk
      secretName: domowik-tls
  rules:
    - host: domowik.lasz.uk
      http:
        paths:
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: backend
                port:
                  number: 8000
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend
                port:
                  number: 80
