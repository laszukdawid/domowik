# Python

For executing Python commands, don't use the global `python3`; instead, use `uv run python`. In case you intend to run a module, e.g. `pytest` then execute `uv run pytest` instead of `python3 -m pytest`.
